let firstvalue="helloname"
let secondvalue="firstvalue"
secondvalue="myname"
console.log(firstvalue)
console.log(secondvalue)
//when we put in stack then we get only the copy of it
//but when we put in the heap we only get the reference of it

